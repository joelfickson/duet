{
  "name": "duet",
  "version": "0.0.1",
  "private": true,
  "description": "Collaborative AI sessions where multiple people share a live context",
  "author": "Joel Fickson",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @duet/server dev & pnpm --filter @duet/client dev",
    "build": "pnpm -r build",
    "lint": "biome lint --write .",
    "format": "biome format --write .",
    "check": "biome check --write .",
    "typecheck": "tsc -b packages/shared packages/server && pnpm --filter @duet/client typecheck",
    "test": "pnpm -r --filter=@duet/server --filter=@duet/client test"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "packageManager": "pnpm@10.28.2",
  "devDependencies": {
    "@biomejs/biome": "^2.4.6"
  }
}
